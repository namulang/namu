pack namu.lang.example.part
aka -> sys.cons

def part
    name := str
        _set(): ok()

    me(newname name): name = newname
    say() void = null

def body from part
    say() void: cons.out("'I'm a body'") // ' inside of a pair of ''.

def button() from part
def mouse() from part
    me(): super('overwrite this')
    me(a new): super() // do nothing

main() void
    cons.out(mouse.name == mouse().name)
    cons.out(mouse(1).name)

arr := part{}
tuple := part;?str // :?
map := part{str}
