인덱스를 0으로 초기화 하는 이유:
인덱스가 0이라는 건, ShortCutSet의 size가 0이 아니라 1이라는 전제조건이 
필요하다는 걸 의미한다.
모든 노드, 모듈은 지정하지 않으면 0으로 초기화가 되기 때문에, 0이라는 건
지정되지 않음(= undefined)를 의미한다.
따라서 0번의 코드값(script, id, priority, namecode, groupcode)는 항상
필요하다.
