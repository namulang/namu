ManagedNodeList 계열은 하위클래스의 insert()를 이용해서 일관성을 유지하는 방법을 사용한다.
그런데, 상위 복사생성자를 호출하면 상위 복사생성자에서는 하위클래스의 insert가 들어가 있는게
아니라 상위클래스의 insert가 들어가 있다.(NEList::insert)
그래서 source로부터의 노드주소값까지 그대로 얇은 복사가 되므로 실패.

따라서, 복사생성자를 호출하지 않고, 그 내용을 직접 하위클래스에서 구현해야 한다.
