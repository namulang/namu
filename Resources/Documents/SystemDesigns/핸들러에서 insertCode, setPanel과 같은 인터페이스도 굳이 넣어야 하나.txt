핸들러에 존재하는 insertCode는 그렇다 쳐도, setPanel같은 경우는 2줄이면 끝난다.
그런데, 그런 코드도 굳이 핸들러에 포함시킬 필요가 있나?

	없다고 생각한다면->
		2줄밖에 안되는 코드때문에 아키텍쳐가 복잡해졌다.
		지금처럼 하면 핸들러 1개, setPanel을 실행하는 모듈1개. 2개가 필요하다.
		그러지 말고 그냥 모듈 1개만들면 되지 않냐.
		
	반박->
		그럴 경우, 핸들러에 정의되어있지 않으므로 스크립트형태의 에디터를 짜지 않고
		코드 형태의 에디터를 만들때 불편하다. (더 복잡해질 것이다)
		게다가 핸들러에 관련있는 메뉴에 필요한 함수들이 모아져 있다면 훨씬 가독성이 
		높을 것이다.
		또한 setPanel을 실행하는 모듈은 핸들러에 상관없이 동작이 가능하므로 
		크게 단점으로 작용하지 않는다.
		
	결론->
		핸들러에 넣읍시다.
