A.dll에 패널만 존재한다.

그리고 B.dll에 모듈과 패널이 같이 있는데, 패널에서 A.Dll을 사용한다고 하자.

그리고 모듈부터 로드를 시작할때 A.dll은 모듈이 없으므로 패스.
B.dll은 A.dll이 로드되어 있어야 하는데 없으므로 에러가 발생한다.

해결방법은, B에서 모듈과 패널을 나누는 것.
A에서 모듈이 없지만 엔트리포인트는 만들어 두는것.
