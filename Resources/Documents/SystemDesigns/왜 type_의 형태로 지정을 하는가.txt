가장 큰 이유는, char 때문이다.
char에 '1' 란 값이 있다면. 이는 문자로써 char를 사용한다는 걸 의미한다.
하지만 때로는 char를 그저 1바이트의 데이터로써 69로 사용하기도 한다.
이를 구분짓기 위해서 NEByteKey가 등장했고.
여기서 나오는 NEUByteKey.getValue()가 무슨 값을 반환하는지 모를 수 있다고
생각해서. type_ubyte를 만들었다. 
안써도 상관은 없다.
