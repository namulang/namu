//def A

def A
    n := 5

b := def B from A
    int[] foo(int max)
        retfun 1..max

res := float if a > 5
    if true
        if true
            for a in 2..foo(5)
                dn = float a + 3
        else
            retfun 0

// comment.
elif a == 5 /* also, comment */
    cout("a=" + a)
    (dn, dmap) := (int[][] [1, 2, foo(3)], int[str] [3:"banana", 3: "apple"])
    dn.len
    dn.each(foo(n))

elif a == 7
    ret a = 0

else
    retif d

