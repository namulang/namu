res := float if a > 5
    if true
        if true
            for a in 2..foo(5)
                dn = 3
// comment.
elif a == 5 /* also, comment */
    cout("a=" + a)
    dn := int[][] [1, 2, foo(3)]
    dmap := int[str] [(2, "banana"), (3, "apple")]

elif a == 7
    a = 0

else
    d
foo()
