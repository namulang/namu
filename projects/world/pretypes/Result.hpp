#pragma once

#include "../msg-usrs/Object.hpp"

namespace wrd
{
	class Result : public Object
	{
		//	TODO: fill this.
	public:
		/*wint warn(.....);
		wint err(....);
		wint info(....);
		wint dbg(..);

		template <typename T>
		T returns() {
			return TNuller<T>::null();
		}
		template <typename T>
		T returns(T value) {
			return value;
		}*/
	};
}
