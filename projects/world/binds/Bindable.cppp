#include "Bindable.hpp"

namespace wrd
{
#define THIS Bindable
	typedef THIS This; //TODO: remove this

    THIS::operator wbool() const { return doesBind(); }

    Result& THIS::bind(Instance* new1)
    {
        WRD_IS_NULL(new1)
        return bind(*new1);
    }

	const Instance& THIS::get() const
	{
		WRD_UNCONST_THIS()
		return unconst->_get();
	}

	Instance& THIS::get() { return _get(); }

#undef THIS
}
