#pragma once

//  Generated by CMake's configure_file():
//      refers .in file to fix.
//          why do we notion like this format? 1 ~~ % 100?:
//              Imagine that today is 8th July. Then CMake kindly generate NE_BUILD_DATE_DAY's value to 08, and boom.
//              beautiful compile error make a grin to you :)
//              To avoid compiler assume that value to "octal" expression, we should do some "tricky" thing.
#define NE_BUILD_DATE_YEAR          @BUILD_DATE_YEAR@
#define NE_BUILD_DATE_MONTH        (1@BUILD_DATE_MONTH@ % 100)  //  == @BUILD_DATE_MONTH@
#define NE_BUILD_DATE_DAY          (1@BUILD_DATE_DAY@ % 100)    //  == @BUILD_DATE_DAY@
#define NE_BUILD_DATE              "@BUILD_DATE@"             
#define NE_BUILD_TIME_HOUR          (1@BUILD_TIME_HOUR@ % 100)    //  == @BUILD_TIME_HOUR@
#define NE_BUILD_TIME_MINUTE        (1@BUILD_TIME_MINUTE@ % 100)  //  == @BUILD_TIME_MINUTE@
#define NE_BUILD_TIME_SECOND        (1@BUILD_TIME_SECOND@ % 100)  //  == @BUILD_TIME_SECOND@
#define NE_BUILD_TIME              "@BUILD_TIME@"
#define NE_BUILD_VERSION_MAJOR      @VERSION_MAJOR@
#define NE_BUILD_VERSION_MINOR      @VERSION_MINOR@
#define NE_BUILD_VERSION_FIX        @VERSION_FIX@
#define NE_BUILD_VERSION            "@VERSION_VALUE@"
#define NE_BUILD_TYPENAME          "@CMAKE_BUILD_TYPE@"
#define NE_BUILD_PLATFORM_NAME      "@CMAKE_SYSTEM_NAME@"
#define NE_BUILD_PLATFORM_VERSION  "@CMAKE_SYSTEM_VERSION@"
#define WRD_LINUX                  1
#define WRD_WINDOWS                2
#ifdef __linux__
#    define WRD_BUILD_PLATFORM        WRD_LINUX
#    define WRD_BUILD_PLATFORM_IS_LINUX
#endif
#ifdef WIN32
#    define WRD_BUILD_PLATFORM        WRD_WINDOWS
#    define WRD_BUILD_PLATFORM_IS_WINDOWS
#endif
