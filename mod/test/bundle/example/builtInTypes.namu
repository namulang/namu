pack namu.lang.example.builtInTypes

_age := 0

main() void
    aka sys.cons -> c
    _age:=0
    ++_age++ *= 2

    pow := _age^3
    pow^^ // age ^ 2

    bitwise := (pow | ~~5 && 0x033 ^^ 0xFF) << 2
    if bitwise & pow > 0
        c.out('okay')
