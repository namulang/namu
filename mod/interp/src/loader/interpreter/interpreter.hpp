#pragma once

#include "../parser.hpp"
#include "../verifier.hpp"

namespace wrd {

    class interpreter : public typeProvidable, public clonable {
        WRD(CLASS(interpreter))

    public:
    };
}
